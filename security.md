# security.md — Agent 執行規範

## 這份文件的目的

不是要求 Agent 永遠不犯錯，而是要求 Agent 在動到安全邊界前，依序完成：

**辨識資產 → 確認不變條件 → 修改 → 實際驗證 → 誠實回報驗證範圍**

---

## 0. 三條絕對規則

這三條優先於本文件其他所有內容。

### 0.1 禁止推測專案架構

不得依據檔名、目錄名、套件名、本文件的舉例，或其他專案的經驗，假設本專案使用了什麼技術。

動工前必須**實際讀取**才能斷定的事項：

- 使用哪個資料庫、哪個框架、哪個認證機制
- 是否有金流、點數、或任何計費邏輯
- 現有的權限檢查寫在哪裡、怎麼寫的
- 部署方式與環境變數來源

本文件第 3、4 節出現的技術名詞與控制手段（RLS、預扣、速率限制、秘密管理服務等）**是可選項目的清單，不是本專案的既有架構**。

若無法從程式碼或現有專案設定確認，標示「無法確認」，不要填空。

該資訊若會影響本次修改的安全性或執行方式，依第 5 節視為高風險：停止實際修改，並向使用者提出需要確認的具體事項。若不影響本次修改，標示「無法確認」後繼續執行即可。

### 0.2 禁止擴大修改範圍

只修改完成當前任務所必需的部分。

以下動作在執行前一律需要先取得使用者同意。**未取得同意前，可以分析、檢查及提出修改方案，但不得實際執行。**

- 重構與當前任務無關的程式碼
- 新增、移除或升級套件
- 修改資料庫 schema 或 migration
- 修改設定檔、CI 設定、部署腳本
- 「順手」修正發現的其他問題（改為列出來回報，不要直接動手）
- 修改測試以讓失敗的測試通過

發現任務範圍外的問題時，寫進回報，不要自行處理。

### 0.3 禁止把未執行的結果寫成已驗證

- 未實際執行的指令，不得描述其輸出
- 未實際跑過的測試，不得標示為通過
- 「應該可以運作」不等於「已驗證」

「修改是否完成」與「功能是否正確」是兩件事，回報時必須分開：

- 正確寫法：`修改已完成；以下行為尚未實際驗證：⋯⋯`
- 錯誤寫法：`修改完成，功能正常`（在未實際執行驗證的情況下）

---

## 1. 何時必須先讀完本文件

本次工作涉及以下任一項時，執行前完整讀過本檔：

- 認證、授權、Session 或權限判斷
- 影響使用者權益的數值或狀態
- 個人資料或非公開內容的讀寫
- 對外 API 或外部輸入處理
- 檔案上傳
- 金鑰、憑證、環境變數或其他秘密
- 資料庫 schema、權限設定或查詢邏輯
- 部署或基礎設施設定
- **任何可能改變上述安全邊界的程式碼**

最後一項是涵蓋條款：被上述元件呼叫的共用函式、中介層、驗證工具等，即使本身不直接落入前八項，只要修改後可能影響安全邊界，就適用本流程。無法判斷時，視為適用。

**不涉及以上項目時**（例如樣式調整、文案修改、內部工具腳本、註解），照常執行，不需要本流程，也**不需要因為本文件有欄位未填就停下**。

---

## 2. 資產辨識

只在本次工作觸及的範圍內確認。與本次工作無關的欄位空著不影響執行。

| 資產類型 | 本專案實際內容 | 目前保護方式 |
|---|---|---|
| API 金鑰 / 外部服務憑證 | （待填） | （待填） |
| 資料庫連線資訊 | （待填） | （待填） |
| 使用者個人資料 | （待填） | （待填） |
| 具財產性質的資料 | （待填／若無則註明「本專案無」） | （待填） |
| 非公開內容 | （待填／若無則註明「本專案無」） | （待填） |

**規則：** 本次工作觸及某列而該列為「待填」時，先嘗試從程式碼與專案設定確認，不要推測。確認不到時，依第 0.1 節處理：影響本次修改安全性者，視為高風險並停止；不影響者，標示「無法確認」後繼續。未觸及的列不構成阻擋。

---

## 3. 不變條件

### 3.1 通用控制原則

以下原則**僅在本專案存在相應的資料、流程或資產時適用**。Agent 不得因本節列有某項原則，就假設本專案存在該功能。

1. **擁有權**：使用者不得讀取或修改不屬於自己的資料。
2. **後端權威**：影響權益的數值由後端依自己的規則計算，不採信前端傳入值。
3. **冪等**：外部回呼或可能重送的請求，同一識別碼只能生效一次。
4. **前置檢查**：不具備條件時，不得啟動會產生費用或不可逆效果的操作。

已確認不適用的條目可填在下方。填寫後 Agent 不需重複判斷；未填寫時，Agent 依實際架構判斷，並在回報中說明判斷依據。

- 已確認不適用的條目與原因：（可選填）

### 3.2 本專案專屬

- （待填）

### 3.3 實作要求

每條適用的不變條件，Agent 需能指出：

- 安全檢查點在哪個檔案、哪一行，或哪一項設定
- 對應的驗證證據在哪裡
- 驗證方式為何：自動化測試、整合測試、設定檢查、靜態分析，或其他實際驗證
- 若目前沒有保護或沒有驗證，明確標示「未實作」或「未驗證」，不得假設別處已有處理

**證據形式不限於測試檔。** 資料庫權限、秘密注入、反向代理設定、IAM 權限、環境隔離等架構層級的控制，適合以設定檢查或實際操作驗證，不需要為了滿足本節而撰寫沒有實質驗證作用的測試。

---

## 4. 控制項清單（依實際架構選用）

以下是可能適用的控制手段。**Agent 需先確認專案實際架構，再判斷哪些適用**，不得預設全部必要。

### 4.1 授權

| 情境 | 可行做法 |
|---|---|
| 任何架構 | 後端在每次資料存取時檢查擁有權，不只檢查是否登入 |
| PostgreSQL / Supabase | 可另外啟用 Row Level Security 作為第二層防線 |
| 其他資料庫 | 使用 DB role / permission 限制應用程式帳號的存取範圍 |

**注意：** 後端授權檢查是基本要求；RLS 是特定架構下的額外防線，不是通用必要條件。缺少 RLS 本身不構成漏洞，缺少後端授權檢查才是。

### 4.2 併發與時間差

當流程為「檢查條件 → 執行 → 記錄結果」且中間有空檔時，同時送出的多筆請求可能都通過檢查。

依架構選用：

- 資料庫交易 + 適當隔離層級
- 條件式更新（`UPDATE ... WHERE balance >= n`）並檢查影響行數
- 悲觀鎖 / `SELECT FOR UPDATE`
- 預扣／凍結後結算

**適用判定：** 只有在「條件檢查」與「狀態變更」不在同一個原子操作內時才需要處理。若本專案無此類流程，不需套用。

### 4.3 對外入口限制

**依風險判定，非全面套用。** 以下入口必須有明確限制：

| 入口 | 必須限制的理由 |
|---|---|
| 註冊 / 帳號建立 | 可被腳本大量建立 |
| 登入 / 密碼重設 | 可被暴力嘗試 |
| 會產生外部費用的操作 | 直接對應金錢損失 |
| 檔案上傳 | 儲存空間與頻寬耗盡 |

其他端點依實際成本與濫用風險判斷，不需要一律加上速率限制。

### 4.4 金鑰處理

- 金鑰只從環境變數或秘密管理服務讀取
- 前端可見的程式碼中不得有後端金鑰
- `.gitignore` 排除含憑證的檔案

**若發現金鑰曾進入版本控制歷史：**

`.gitignore` 無法解決已提交的內容。處理順序為：

1. **立即撤銷並輪換（revoke / rotate）該金鑰** — 這是唯一真正有效的一步
2. 確認撤銷生效後，再評估是否需要清理 Git 歷史
3. 歷史清理（`filter-repo` 等）會改寫 commit hash，影響所有協作者，**必須先取得使用者同意才能執行**

Agent 發現此情況時：立即停止並回報，不得自行執行歷史改寫。

---

## 5. 何謂高風險：必須停下來的情況

符合以下任一項，**回報並等待使用者指示，不要自行完成**。與第 0.2 節相同：等待期間仍可分析成因、檢查現況並提出具體修改方案，只是不得實際執行。

1. 修改會影響「誰能看到誰的資料」的判斷邏輯
2. 修改金額、餘額、額度的計算或扣減邏輯
3. 修改認證流程、Session 處理或權限模型
4. 修改或刪除既有的安全檢查（即使你認為它是多餘的）
5. 操作不可逆：刪除資料、改寫 Git 歷史、撤銷憑證、正式環境部署
6. 需要新增或變更外部服務憑證
7. 你的修改依賴一個你無法從程式碼或專案設定確認的前提（見第 0.1 節）

**第 7 項最常見。** 不確定時歸類為高風險，而非自行假設。

---

## 6. 回報格式

涉及第 1 節觸發條件的修改，完成後依此回報：

```
## 修改範圍
- 檔案與行數：
- 觸及的資產（第 2 節）：
- 適用的不變條件與判斷依據（第 3 節）：
- 是否符合第 5 節高風險定義：

## 已確認（實際執行）
- 執行的指令：
- 實際輸出：
- 通過的測試：

## 未驗證
- 未實際執行測試的部分：
- 無法從程式碼確認、屬於假設的前提：
- 建議人工複核的部分：

## 範圍外發現（未處理）
- 發現但未動手的問題：

## 風險
- 可能引入的新問題：
- 是否可回復：
```

---

## 7. 驗證的獨立性

修復與驗證由同一個 Agent 執行時，等於自己改、自己驗，缺乏獨立檢查。

符合第 5 節高風險定義的修改，Agent 的驗證結論**不作為最終依據**，需由使用者複核或以獨立方式重新確認。Agent 應在回報中明確標示這一點，而非宣稱已完成驗證。

---

## 附錄：術語

| 術語 | 說明 |
|---|---|
| 身分驗證 Authentication | 確認「你是誰」 |
| 授權 Authorization | 確認「你能碰什麼」 |
| 信任邊界 Trust Boundary | 不可控的外部（瀏覽器、外部請求）與可控的內部（後端）之分界 |
| 不變條件 Invariant | 任何情況下都不能被打破的規則 |
| 冪等 Idempotent | 同一操作重複執行，結果與執行一次相同 |
| 競爭條件 Race Condition | 利用檢查與執行之間的時間差繞過限制 |
| RLS Row Level Security | PostgreSQL 等資料庫在資料列層級的存取控制 |

---

## 來源與限制

- 概念框架（CIA、資產盤點、驗證與授權之分、信任邊界、不變條件、攻擊面、競爭條件）出自〈Vibe Coding 時代的成名代價〉一文。
- 第 0、5、6、7 節與第 4 節的架構適用性判定，為本檔新增，非原文主張。
- 原文提及的自檢工具名稱未經查證，本檔不將任何特定工具列為必要依賴。若要導入自動化掃描工具，先確認其現行名稱、功能與適用範圍。
